---
interface Props {
	date: Date;
	class: string
}

const { date, class: classList } = Astro.props;
---

<time datetime={date.toISOString()} class={classList}>
	{
		date.toLocaleDateString('ca-es', {
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		})
	}
</time>
